# T-INFRA-11: Docker ignore — exclude dev artifacts from image context

# Build output
**/bin/
**/obj/

# Local settings (secrets must come from Key Vault)
**/appsettings.Development.json
**/appsettings.Local.json
**/*.user

# Tests excluded from final image via multi-stage build, but .csproj/.cs needed
# during `dotnet restore GOP.slnx` because the solution references test projects
# tests/

# Docker files
**/Dockerfile*
**/.dockerignore

# Git
.git/
.gitignore
.gitattributes

# IDE
.vscode/
.vs/
*.suo
*.user
.idea/

# Logs
logs/
*.log

# Coverage
coverage/
TestResults/

# Docker compose (dev only)
docker-compose*.yml
